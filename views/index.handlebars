<div>
    <!--img src="../public/assets/img/burger.jpg" alt="burger"-->
    <img src="/assets/img/burger.jpg" alt="burger">
    <!--href="/assets/css/burger_style.css"-->

</div>

<h2>Eat-Da-Burger!</h2>

       
<div class="col-md-6">

<ol>
  {{#each burgers}}
    {{#unless this.devoured}}
      <li>
        {{this.burger_name}}
      
        <form action="/{{this.id}}?_method=PUT" method="POST">
          <input type="hidden" name="devoured" value="true">
          <button type="submit">Devour it!</button>
        </form>
       
      </li>
    {{/unless}}
  {{/each}}
</ol>

</div>

<div class="col-md-6">
</div>

<div class="col-md-5">

  {{#each burgers}}
    {{#unless this.devoured}}
      
        {{this.burger_name}}

      {{/unless}}
  {{/each}}

</div>
<div class="col-md-1">

  {{#each burgers}}
    {{#unless this.devoured}}
      
        <form action="/{{this.id}}?_method=PUT" method="POST">
          <input type="hidden" name="devoured" value="true">
          <button type="submit">Devour it!</button>
        </form>
    
    {{/unless}}
  {{/each}}

</div>

<div class="col-md-6">
</div>

<div class="col-md-12">
<!-- should this be action="/"? -->
<form class="create-update-form" action="/index" method="POST">

  <div class="form-group">
    <!-- change id="ca" to what?-->
    <!--input type="text" id="ca" name="name"-->
    <input type="text" name="burgerText">
  </div>


  <button type="submit">Submit</button>
</form>

</div>

